export type UserData = {
  id: string;
  email: string;
  last_name: string;
  first_name: string;
  patronymic?: string;
  telegram: boolean;
  is_student: boolean;
  avatar_url: string;

  // coworking booking id ?
}
